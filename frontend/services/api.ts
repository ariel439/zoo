// services/api.ts

/**
 * This file will contain all the logic for making API calls to your Spring Boot backend.
 * By centralizing API requests here, you keep your components clean and focused on UI.
 *
 * Example function:
 *
 * export const getAnimals = async () => {
 *   try {
 *     const response = await fetch('/api/animais');
 *     if (!response.ok) {
 *       throw new Error('Network response was not ok');
 *     }
 *     return await response.json();
 *   } catch (error) {
 *     console.error("Failed to fetch animals:", error);
 *     throw error;
 *   }
 * };
 *
 */
